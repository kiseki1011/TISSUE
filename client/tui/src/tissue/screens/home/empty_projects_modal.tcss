EmptyProjectsModal {
    align: center middle;
    background: $background 60%;
}

#empty-projects-dialog {
    width: 56;
    max-width: 90%;
    min-width: 44;
    height: auto;
    min-height: 10;
    max-height: 90%;
    padding: 0;
    border-title-align: center;
    border-title-color: $accent;
    border-subtitle-align: center;
    border-subtitle-color: $text-muted;
}

#empty-projects-form {
    width: 100%;
    height: auto;
    padding: 2 4;

    #empty_projects_message {
        width: 100%;
        height: auto;
        padding: 0 0 1 0;
        color: $text;
        text-align: left;
    }

    #empty-projects-buttons {
        width: 100%;
        height: auto;
        align-horizontal: right;
        margin-top: 1;

        Button {
            margin-left: 1;
            min-width: 12;
        }
    }
}
