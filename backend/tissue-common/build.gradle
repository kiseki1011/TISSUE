project(':tissue-common') {
  dependencies {
    api libs.spring.web

    api libs.commons.lang3
    api libs.commons.text

    api libs.jspecify

    implementation libs.jackson.databind.nullable
  }

  bootJar.enabled = false
  jar.enabled = true
}
