project(':tissue-bootstrap') {
  dependencies {
    implementation project(':tissue-api')
    implementation project(':tissue-infra')

    // implementation libs.spring.boot.starter.actuator
  }

  bootJar.enabled = true
  jar.enabled = false
}
